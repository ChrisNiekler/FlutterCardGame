import 'package:wizard2/card.dart';

class Deck {
  List<Card> cards = new List(60);
  void distribution() {}
  void shuffleCards() {}
}

Deck createDeck() {
  // clubs (♣), diamonds (♦), hearts (♥) and spades (♠)
  Deck output;
  // first loop for the four different types
  for (int i = 0; i < 4; i++) {
    // second loop for the fourteen different cards
    for (int j = 0; j < 14; j++) {}
  }
}
