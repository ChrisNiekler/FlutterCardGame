import 'package:flutter/animation.dart';
import 'package:flutter/material.dart';

Widget scoretable() {
  return AlertDialog(
    content: Column(
      children: <Widget>[
        Container(
          child: Text("hallo"),
        ),
      ],
    ),
  );
}